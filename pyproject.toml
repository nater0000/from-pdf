[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pdf-to-md"
version = "1.0.0"
description = "Serverless PDF to Markdown extraction via GitHub Issues"
dependencies = [
    "marker-pdf>=2.0.0"
]
